'use strict';

/**
 * Shared runtime state (single instance via Node require cache).
 * Use sparingly for values that really must be shared across modules.
 */
module.exports = {
  embeddingDimension: 1536,
};
